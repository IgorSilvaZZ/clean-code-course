// Simples

// setTimeout, setInterval

const INTERVAL_30_DAYS = 1000 * 60 * 60 * 24 * 30;

setTimeout(() => {}, INTERVAL_30_DAYS);

// ====================

// Avançado

function calculateDiscount(priceInCents, discountAmountPercentage) {
  // Retorna desconto
}
