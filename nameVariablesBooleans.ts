// Booleanos

// Prefixos das variaveis
// is, does, has

const isUserMajority = true;
const isDisabled = true;

const userHasGroup = false;

// ===================

if (isDisabled) {
  console.log({ isDisabled });
}
